---
controller_templates:
  - name: build_execution_environments
    job_type: run
    inventory: config_as_code
    execution_environment: supported
    project: config_as_code
    playbook: playbooks/build_ee.yml
    credentials:
      - ah_admin_user_pass
      - root

  - name: ah_config
    job_type: run
    inventory: config_as_code
    execution_environment: supported
    project: config_as_code
    playbook: playbooks/hub_config.yml
    credentials:
      - ah_token_user

  - name: controller_config
    job_type: run
    inventory: config_as_code
    execution_environment: supported
    project: config_as_code
    playbook: playbooks/controller_config.yml
    credentials:
      - aap_admin
      - ah_token_user
      - vault

  - name: test_rhelconfigure
    job_type: run
    inventory: insights_inventory
    execution_environment: Default execution environment
    project: fedoralaptop
    playbook: rhel/rhelconfigure.yml
    #credentials:
      #- aap_admin
      # - ah_token_user
      #- vault

  - name: test_rhelunregister
    job_type: run
    inventory: insights_inventory
    execution_environment: Default execution environment
    project: fedoralaptop
    playbook: rhel/rhelunregister.yml
    #credentials:
      #- aap_admin
      #- ah_token_user
      #- vault
...